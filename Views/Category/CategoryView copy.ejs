<%- include('../header') %>
  <%- include('../sidebar') %>

    <main id="main" class="main">

      <div class="pagetitle">
        <h1>Category</h1>
        <a style="float: right;" href="/category/Add"><i class="bi bi-file-earmark-plus"></i>
          New Caetgory</a>
        <nav>
          <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="index.html">Home</a></li>
            <li class="breadcrumb-item">Caegory</li>
            <li class="breadcrumb-item active">List</li>
          </ol>
        </nav>
      </div><!-- End Page Title -->

      <% if (messages.success) { %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <i class="bi bi-check-circle me-1"></i>
          <%= messages.success %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>

        <% } %>

          <section class="section">
            <div class="row">
              <div class="col-lg-12">
                <div class="card">
                  <div class="card-body">
                    <h5 class="card-title">Default Table</h5>

                    <!-- Default Table -->
                    <table class="table">
                      <thead>
                        <tr>
                          <th scope="col">#</th>
                          <th scope="col">Title</th>
                          <th scope="col">Description</th>
                          <th scope="col" colspan="2">Action</th>
                        </tr>
                      </thead>
                      <tbody>
                        <% if (Category.length> 0) { %>
                          <% Category.map((cat, i)=> { %>
                            <tr>
                              <th scope="row">
                                <%= i+1 %>
                              </th>
                              <td>
                                <%= cat.title %>
                              </td>
                              <td>
                                <%= cat.description %>
                              </td>
                              <td><a href="/Category/editCategory/<%= cat._id %>"><i class="bi bi-pencil-square"></i>
                                </a></td>
                              <td><a href="/Category/delete/<%= cat._id %>" class="DeleteOperationCategory"><i
                                    class="bi bi-file-earmark-x bi-xl"></i></a></td>
                            </tr>
                            <% }) %>
                              <% } else { %>
                                <tr>
                                  <td colspan="5">No categories available.</td>
                                </tr>
                                <% } %>


                      </tbody>
                    </table>
                    <!-- End Default Table Example -->
                  </div>
                </div>
              </div>
            </div>
          </section>
    </main>

    <main id="main" class="main">
      <div>

        <h1>Add Categfory - <a href="/Category/Add">Add</a> </h1>
        <h1>Products - </h1>
        <div>
          <% if (messages.success) { %>
            <div class="alert alert-success">
              <%= messages.success %>
            </div>
            <% } %>
        </div>


        <ul>
          <% if (Category.length> 0) { %>
            <% Category.map((cat, i)=> { %>
              <li>
                <span>
                  <%= i+1 %>--<%= cat.title %>: <%= cat.description %>
                </span>
                <a href="/Category/editCategory/<%= cat._id %>">Edit </a>
                || <a href="/Category/delete/<%= cat._id %>" class="DeleteOperationCategory">Delete</a>
              </li>
              <% }) %>
                <% } else { %>
                  <p>No categories available.</p>
                  <% } %>
        </ul>


      </div>
    </main>




    <%- include('../footer') %>